# Redis connection used for caching analytics responses.
# Copy this file to `.env` and fill in your own values.
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# Optional server tuning (used by serve.py / gunicorn)
MODEL_SERVER_WORKERS=1
MODEL_SERVER_TIMEOUT=60
